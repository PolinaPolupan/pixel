spring:
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB

storage.directory: upload-image-dir
node.service.url: node-service-url

logging:
  level:
    root: INFO
    com.example.mypixel: DEBUG
    org.springframework.web: DEBUG