spring:
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB

storage:
  images: upload-image-dir
  temp-images: temp-image-dir

logging:
  level:
    org.springframework.web: DEBUG