spring:
  servlet:
    multipart:
      max-file-size: 8MB
      max-request-size: 8MB

  data:
    redis:
      host: redis
      port: 6379

storage:
  images: upload-image-dir

logging:
  level:
    org.springframework.web: DEBUG